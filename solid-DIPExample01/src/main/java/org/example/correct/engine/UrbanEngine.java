package org.example.correct.engine;

import org.example.correct.abstration.Engine;

public class UrbanEngine extends Engine {
    protected UrbanEngine(Integer cylinder) {
        super(cylinder);
    }
}
