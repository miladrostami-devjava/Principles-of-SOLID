package org.example.correct.engine;

import org.example.correct.abstration.Engine;

public class SportEngine extends Engine {
    public SportEngine(Integer cylinder) {
        super(cylinder);
    }
}
